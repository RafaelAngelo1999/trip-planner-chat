# LangGraph Configuration - REQUIRED
# These configurations are now mandatory and the app will always use them
NEXT_PUBLIC_API_URL=http://localhost:2024
NEXT_PUBLIC_ASSISTANT_ID=agent

# LangSmith API Key (optional for local development)
# Do NOT prefix this with "NEXT_PUBLIC_" as we do not want this exposed in the client.
LANGSMITH_API_KEY=

# Production LangGraph Configuration Example
# NEXT_PUBLIC_API_URL="https://my-website.com/api"
# NEXT_PUBLIC_ASSISTANT_ID="agent"
# LANGSMITH_API_KEY="lsv2_..."

# Note: Language selection is now available in the main UI and no longer configured here
